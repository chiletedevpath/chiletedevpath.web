---
const { project } = Astro.props;
const isEnglish = project.action?.toLowerCase().startsWith("view");
const labels = isEnglish
  ? { problem: "Problem", learned: "What I learned", improve: "What I would improve" }
  : { problem: "Problema", learned: "Qué aprendí", improve: "Qué mejoraría" };
---

<article
  class="tarjeta tarjeta-con-enlace"
  data-project-card={project.category ? "true" : undefined}
  data-project-category={project.category}
>
  {project.imageSrc ? (
    <figure class="proyecto-visual proyecto-visual-imagen">
      <img src={project.imageSrc} alt={project.imageAlt ?? ""} loading="lazy" />
      {project.visual && <figcaption>{project.visual}</figcaption>}
    </figure>
  ) : project.visual && (
    <div class="proyecto-visual">
      <span>{project.visual}</span>
    </div>
  )}
  <span class="estado">{project.status}</span>
  {project.level && <span class="proyecto-nivel">{project.level}</span>}
  {typeof project.progress === "number" && (
    <div class="avance-proyecto" aria-label={`Avance del proyecto ${project.progress}%`}>
      <div class="avance-proyecto-barra">
        <span style={`width: ${project.progress}%`}></span>
      </div>
      <strong>{project.progress}%</strong>
    </div>
  )}
  <h3>{project.title}</h3>
  <p>{project.description}</p>
  {Array.isArray(project.technologies) && (
    <div class="proyecto-tech" aria-label="Tecnologías del proyecto">
      {project.technologies.map((tech) => <span>{tech}</span>)}
    </div>
  )}
  <ul class="tarjeta-detalles">
    {project.details.map((detail) => <li>{detail}</li>)}
  </ul>
  {(project.problem || project.learned || project.improve) && (
    <div class="proyecto-ficha">
      {project.problem && (
        <div>
          <span>{labels.problem}</span>
          <p>{project.problem}</p>
        </div>
      )}
      {project.learned && (
        <div>
          <span>{labels.learned}</span>
          <p>{project.learned}</p>
        </div>
      )}
      {project.improve && (
        <div>
          <span>{labels.improve}</span>
          <p>{project.improve}</p>
        </div>
      )}
    </div>
  )}
  <a class="boton boton-principal" href={project.href} target="_blank" rel="noopener noreferrer">
    {project.action}
  </a>
</article>
